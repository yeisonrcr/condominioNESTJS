# ============================================
# BASE DE DATOS POSTGRESQL
# ============================================
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/rosedal2?schema=public"

# ============================================
# REDIS
# ============================================
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""

# ============================================
# JWT - GENERA CON: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# ============================================
JWT_SECRET="CAMBIAME_POR_SECRETO_REAL_EN_PRODUCCION_64_CARACTERES_MINIMO"
JWT_REFRESH_SECRET="CAMBIAME_REFRESH_TOKEN_SECRET_DIFERENTE_64_CARACTERES"
JWT_EXPIRATION="1h"
JWT_REFRESH_EXPIRATION="7d"

# ============================================
# CIFRADO DE FIRMAS - GENERA CON: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ============================================
SIGNATURE_ENCRYPTION_KEY="CAMBIAME_32_BYTES_HEX_64_CARACTERES_PARA_AES256"

# ============================================
# CORS - DOMINIOS PERMITIDOS
# ============================================
CORS_ORIGINS="http://localhost:3000,http://localhost:3001,http://localhost:3002"

# ============================================
# APLICACIÓN
# ============================================
NODE_ENV="development"
PORT=3001

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
